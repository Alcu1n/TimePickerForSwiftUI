# Folder: Tests/WheelPickerKitTests

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时,更新此文档。

This folder verifies the shipped package API with small deterministic tests. / 本目录通过小而确定的测试验证已分发的包 API。  
It should cover presets and public arc-profile configuration, not package-private rendering internals. / 它应覆盖预设与公开圆弧轮廓配置，而不是触碰包内私有渲染细节。  
The invariant is that renderer rewrites do not break tests unless the public picker contract changes. / 最重要的不变量是除非公开选择器契约变化，否则渲染器重写不应破坏这些测试。

## Files
- `TimerWheelPickerTests.swift`: test file - Public API, preset, and arc-profile configuration checks / 公开 API、预设与圆弧轮廓配置检查
- `.folder.md`: local map - Folder responsibilities and file list / 目录职责与文件列表
