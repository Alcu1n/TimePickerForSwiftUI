# Folder: Tests/WheelPickerKitTests

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时,更新此文档。

This folder verifies the shipped package API with small deterministic tests. / 本目录通过小而确定的测试验证已分发的包 API。  
It should touch public configuration and picker contract, not package-private rendering internals. / 它应只触达公开配置与选择器契约，不触碰包内私有渲染细节。  
The invariant is that every test remains stable across UI skin changes as long as the public API stays valid. / 最重要的不变量是只要公开 API 有效，UI 皮肤变化不应破坏这些测试。

## Files
- `TimerWheelPickerTests.swift`: test file - Public API and default configuration checks / 公开 API 与默认配置检查
- `.folder.md`: local map - Folder responsibilities and file list / 目录职责与文件列表
