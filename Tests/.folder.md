# Folder: Tests

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时,更新此文档。

This folder holds package-level verification only. / 本目录只承载包级别验证代码。  
It depends on `Sources/` and must not pull demo app behavior from `WheelPicker/`. / 它依赖 `Sources/`，不得引入 `WheelPicker/` 中的示例应用行为。  
The invariant is that tests validate the public package contract, not private demo presentation. / 最重要的不变量是测试验证公开包契约，而不是私有示例表现层。

## Files
- `WheelPickerKitTests/`: test target - Public API verification for the package / 包公开 API 的验证目标
- `.folder.md`: local map - Folder responsibilities and file list / 目录职责与文件列表
