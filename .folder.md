# Folder: /

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时,更新此文档。

This folder defines the project boundary and canonical map. / 本目录定义项目边界与总地图。  
It passes distributable module ownership into `Sources/`, demo app ownership into `WheelPicker/`, and app build metadata into `WheelPicker.xcodeproj/`. / 它将可分发模块职责交给 `Sources/`，将示例应用职责交给 `WheelPicker/`，并将应用构建元数据交给 `WheelPicker.xcodeproj/`。  
The invariant is that docs and source structure stay aligned after every package, app, or component API change. / 最重要的不变量是每次包结构、应用结构或组件 API 变更后文档与源码结构必须保持一致。

## Files
- `README.md`: root - Project overview and sync protocol / 项目概览与同步协议
- `.folder.md`: root - Folder-level map for the repository root / 仓库根目录的局部地图
- `.gitignore`: root - Ignore local Xcode and SwiftPM noise before GitHub push / 在推送 GitHub 前忽略本地 Xcode 与 SwiftPM 噪音
- `Package.swift`: root - Swift Package manifest for reusable distribution / 用于可复用分发的 Swift Package 清单
- `Sources/`: root child - Swift Package source modules / Swift Package 源码模块
- `Tests/`: root child - Package-level public API verification / 包级公开 API 验证
- `WheelPicker/`: root child - App source, UI, and resources / 应用源码、界面与资源
- `WheelPicker.xcodeproj/`: root child - Xcode project configuration / Xcode 工程配置
